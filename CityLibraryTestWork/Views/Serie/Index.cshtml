@model IEnumerable<CityLibraryTestWork.Models.Serie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SerieName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SerieName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SerieId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SerieId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SerieId })
        </td>
    </tr>
}

</table>
